export const APIConfig = {
	// baseURL: 'http://10.0.2.2:3001/', // if using android emulator
	baseURL: 'http://localhost:3001/', // if using real device
	headers: {
		'Content-Type': 'application/json',
		'Access-Control-Allow-Origin': '*',
	},
};
